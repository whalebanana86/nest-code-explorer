# Changelog

## 0.3.2 (2026-09-17)

- 기본 배율을 올렸다: 최소 0.95(이전 0.65), 전체 맞춤 상한 1.2(이전 1).
- 미니맵: 크기 260×180, 보는 영역 사각형 안에서 누르면 점프 없이 절반 속도로 끌고, 밖을 누르면 애니메이션으로 이동.
- 실행 경로: HTTP 메서드별 색(GET 파랑 · POST 초록 · PUT 주황 · PATCH 청록 · DELETE 빨강)을 왼쪽 목록·요약 배지·메서드 상세 배지에 적용.

## 0.3.1 (2026-09-17)

- 전체 맞춤 때 글자를 읽을 수 없는 배율(0.65 미만)로는 줄이지 않는다. 그 대신 왼쪽 위부터 보여 주고 나머지는 미니맵으로 안내한다.
- 오른쪽 아래 **미니맵**: 전체 노드와 지금 보는 영역(사각형). 화면 밖에 노드가 있을 때만 나타나고, 누르거나 끌면 그곳으로 이동한다.
- 노드 클릭·펼치기·접기·검색 이동 때 전체를 다시 맞추지 않는다. 새로 생긴 노드가 화면 밖일 때만 그만큼 이동(필요하면 최소한으로 축소)한다. 줌인해 둔 상태에서 노드를 누르면 줌아웃되던 문제 해결.

## 0.3.0 (2026-09-17)

- 구조 모드 왼쪽 창에 **폴더로 펼치기**: `src/` 아래 `folderDepth`(기본 2) 단계 폴더별 클래스 수를 보여 주고 체크한 폴더의 클래스만 그래프에 올린다. 진입점에서 닿지 않는 노드도 자기 열에 배치한다.
- **초점 모드**(헤더 `초점` 버튼 / 왼쪽 체크박스): 고른 노드로 들어오는 것 + 거기서 나가는 것만 남기고 나머지는 숨긴다.
- **전부 펼치기 임계값**: 클래스가 60개를 넘으면 안내를 띄우고 폴더 목록을 연다("그래도 펼치기" 링크로 강제 가능).
- 보이는 노드끼리의 의존 간선은 펼침 여부와 상관없이 항상 그린다 (이전에는 펼친 노드에서 나가는 간선만).
- `--init` 기본 계층에 `Usecase`(`*.usecase.ts`, 1열) 추가. 설정 키 `folderDepth` 추가.
- 왼쪽 목록 토글(◧)이 구조 모드에서도 동작한다.

## 0.2.4 (2026-09-17)

- 실행 경로 모드의 테이블 노드를 높이 고정 원통으로 직접 그린다. vis 의 `database` 모양은 라벨이 길수록 높이도 커져서 테이블끼리 겹치고 글자 양끝이 잘렸다.

## 0.2.3 (2026-09-17)

- README 사용법을 실행 순서(실행 → 설정 → 반복)로 다시 썼다. 코드 변경 없음.

## 0.2.2 (2026-09-17)

- 탐색기 제목이 템플릿에 박혀 있던 것("Push Platform 코드 탐색기")을 설정 `title` → `package.json` name → 폴더 이름 순으로 정한다.
- README 를 왜 만들었나 · 사용법 · 화면과 기능(스크린샷) 중심으로 다시 썼다. 스크린샷은 `scripts/readme-shots.js` 로 재생성한다.

## 0.2.1 (2026-09-17)

- `examples/order-app` 예시 프로젝트 추가 (manual-router 큐, RawSQL/TypeORM, cron, AppError, 외부 시스템, Guard). README 의 "push-platform 설정 참고" 를 이 예시로 대체.
- `test/example.test.js` 가 예시 분석 결과(라우트·큐 경계·테이블·에러·외부·cron)를 고정한다.

## 0.2.0 (2026-09-17)

- `--init [--force]`: 기본값 전체를 `code-explorer.config.json` 으로 생성 (`$comment` 에 섹션 설명). 이미 있으면 `--force` 없이는 건드리지 않는다.
- 설정 파일이 없으면(`--config` 로 지정하지 않은 경우) 기본값으로 실행한다. 이전에는 `config not found` 로 종료했다.
- 출력 디렉터리(`docs/`, `docs/diagrams/`)가 없으면 만든다. 이전에는 새 프로젝트에서 ENOENT 로 실패했다.
- `run({ configPath })` 가 선택 인자가 됐고 `defaultConfig()` / `initConfig()` 를 export 한다.

## 0.1.0 (2026-09-17)

- 첫 배포: 구조 모드·실행 경로 모드 탐색기(단일 HTML), mermaid 코드 지도, 큐 어댑터(manual-router / nestjs-bullmq / none).
